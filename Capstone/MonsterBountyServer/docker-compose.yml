version: "3.8"
services:
  postgres-db:
    container_name: monster_bounty_server
    image: postgres # use latest official postgres version
    restart: always
    environment:
      POSTGRES_USER: monster
      POSTGRES_PASSWORD: bounty
      POSTGRES_DB: monsterbounty
      POSTGRES_HOST_AUTH_METHOD: password
    ports:
      - "5436:5432"

volumes:
  postgresql_data:
    driver: local